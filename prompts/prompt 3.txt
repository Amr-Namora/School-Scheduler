Build the SchoolScheduler.Infrastructure project, implementing the
interfaces defined in Application.

IDENTITY:
- Use ASP.NET Core Identity with a dual-DbContext pattern (same as our
  earlier DLMS project): one DbContext for business entities
  (SchoolSchedulerDbContext), one for Identity (IdentityDbContext with a
  custom ApplicationUser : IdentityUser that adds a FullName property).
- Implement IUserLookupService (from Application) using
  UserManager<ApplicationUser> — FindUserIdByEmailAsync via
  FindByEmailAsync, GetFullNameAsync via FindByIdAsync then reading
  FullName.

PERSISTENCE:
- EF Core DbContext with DbSets for all Domain entities, including the
  new TeacherLinkRequest.
- Fluent API configurations (OnModelCreating) for relationships and keys.
- Global query filter on every tenant-scoped entity so queries are
  automatically scoped to SchoolId == current school (use an
  ICurrentSchoolContext service, injected, populated by the Api layer
  from the authenticated user). ICurrentSchoolContext should resolve the
  current SchoolId by looking up School where School.UserId == the
  authenticated user's id (cache per request so it's not queried
  repeatedly).
- ADDITIONAL global query filter on School: exclude rows where
  IsDeleted == true, by default.
- EF Core migrations setup.

SCHEDULING SOLVER (this is the core of the project):
- Implement ITimetableGenerationService using the Google.OrTools NuGet
  package's CP-SAT solver.
- Model each (ClassRoom, Day, Slot) as a decision variable domain over
  possible (Subject, Teacher) pairs, constrained to:
  1. HardConstraint: teacher not double-booked at same (Day, Slot) across
     all classes they're assigned to
  2. HardConstraint: teacher only assigned within their declared
     TeacherAvailability
  3. HardConstraint: sum of a subject's occurrences per class per week
     equals its WeeklyQuota exactly
  4. HardConstraint: a subject appears at most 2 times per day per class
  5. HardConstraint: no teacher has 3+ consecutive slots (same day) with
     no break between them — model this as a sliding window constraint
     over consecutive slot numbers
  - If the solver finds the problem infeasible, return a result object
    that explains which constraint category likely caused it (as far as
    OR-Tools' infeasibility info allows), not just "failed."
  - Keep the solver logic in its own well-named class(es), separate from
    EF Core code, so it stays testable in isolation.

Explain briefly, after generating, how you modeled the CP-SAT variables
and constraints (2-3 sentences), so I can sanity-check the approach before
we move to the API layer.