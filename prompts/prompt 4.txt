Build the SchoolScheduler.Api project, wiring up Domain + Application +
Infrastructure.

- Controllers (or minimal API endpoints) for every Command/Query defined
  in Application: school setup/onboarding endpoints, teacher/availability
  endpoints, class/assignment endpoints, GenerateTimetable, and the two
  read endpoints (class timetable, teacher timetable).
- Authentication/authorization: each authenticated principal belongs to
  either a School (admin), a Teacher, or a Student tied to a ClassRoom.
  Implement ICurrentSchoolContext (from Infrastructure) so tenant scoping
  works automatically. Use ASP.NET Core Identity with JWT — same pattern
  as our earlier DLMS project if you're already familiar with it.
- DI composition: wire MediatR, FluentValidation, AutoMapper, DbContext,
  and the OR-Tools solver service in Program.cs.
- Swagger/OpenAPI enabled for manual testing.
- DTOs/response shapes: class timetable and teacher timetable responses
  should return a clean day-by-slot grid structure (not raw entities),
  and the teacher timetable response must include the School name/id per
  entry since a teacher can work at multiple schools.

After generating, list all endpoints created with their routes and HTTP
methods, and confirm the project builds and Swagger comes up.